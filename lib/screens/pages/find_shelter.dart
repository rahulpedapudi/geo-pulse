import 'package:flutter/material.dart';

class FindShelter extends StatelessWidget {
  const FindShelter({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("Find Shelter")));
  }
}
